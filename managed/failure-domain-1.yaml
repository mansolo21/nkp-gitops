apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: NutanixFailureDomain
metadata:
  name: fd-1                # must be unique per namespace
  namespace: nkp-pro-ex-01-lq9q6     # same namespace as the cluster
spec:
  prismElementCluster:
    type: name
    name: kestrel02-3
  subnets:
    - type: name
      name: nkp-1
